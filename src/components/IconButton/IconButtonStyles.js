import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        width: 50,
        aspectRatio: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
});
