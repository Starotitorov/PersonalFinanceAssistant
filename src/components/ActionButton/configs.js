import { ADD, SAVE } from './types';

export default {
    [ADD]: {
        iconName: 'add',
        color: '#fff',
        backgroundColor: '#f00'
    },
    [SAVE]: {
        iconName: 'done',
        color: '#fff',
        backgroundColor: '#f00'
    }
}
