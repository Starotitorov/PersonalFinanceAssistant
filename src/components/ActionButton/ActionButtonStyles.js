import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        position: 'absolute',
        right: 16,
        bottom: 16
    },
    iconStyle: {
        fontSize: 32
    },
});
