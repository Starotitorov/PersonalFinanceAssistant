import { StyleSheet } from 'react-native';
import { fontSizes } from 'src/styles';

export default StyleSheet.create({
    container: {
        flex: 1
    },
    chart: {
        flex: 1
    },
    view: {
        flex: 1
    }
});
