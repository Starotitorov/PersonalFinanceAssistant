import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  subItemContainer: {
    flexDirection: 'row',
    padding: 10,
    alignItems: 'center',
    width: '100%',
    backgroundColor: '#aaa'
  },
  subItemDateContainer: {
    flex: 1
  }
});
